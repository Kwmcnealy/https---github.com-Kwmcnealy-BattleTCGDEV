/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [],
    remotePatterns: [],
  },
  reactStrictMode: true,
};

module.exports = nextConfig; 